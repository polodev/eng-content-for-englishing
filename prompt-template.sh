#!/bin/bash

# Adjust `claude` command to your actual CLI (e.g., `claude`, `claude-code`, `llm`, etc.)

claude <<'EOF'
hello
EOF

sleep 5  # optional wait

claude <<'EOF'
hello
EOF

sleep 5  # optional wait

claude <<'EOF'
hello
EOF

